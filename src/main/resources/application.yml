micronaut:
        application:
                name: box
        server:
                max-request-size: '100MB'
                multipart:
                        max-file-size: '100MB'

netty:
        default:
                allocator:
                        max-order: 3